plugins {
    id 'scala'
}

dependencies {
    implementation project(':common:scala')
    implementation project(':core:test') // It might depend on core:test
    implementation "org.scala-lang:scala-library:${gradle.scala.version}"
    implementation "org.scalatest:scalatest_${gradle.scala.version}:${gradle.scalatest.version}"
    implementation "com.typesafe.akka:akka-actor_${gradle.scala.version}:${gradle.akka.version}"
    implementation "com.typesafe.akka:akka-testkit_${gradle.scala.version}:${gradle.akka.version}"
    implementation project(':core:containerpool') // It likely depends on the main containerpool code
    // Add any other specific dependencies for containerpool testing
}
